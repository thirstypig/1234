---
import { getLangStaticPaths, type Lang } from '../../i18n/locales';
import Page from '../../features/home/Page.astro';

export const getStaticPaths = getLangStaticPaths;
const lang = Astro.params.lang as Lang;
---
<Page lang={lang} />
