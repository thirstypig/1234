# Grid pattern

The 作文簿 / 田字格 exercise-book grid is a design element with rules: section backgrounds (hero, How it works), step number boxes, and Chinese display/H2 headlines one character per box. Never behind body text — reading copy always sits on a white `surface` card.
