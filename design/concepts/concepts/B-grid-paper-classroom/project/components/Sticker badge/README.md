# Sticker badge

Short trust facts as stickers slapped on the page: white die-cut edge, small tilt (−5° to +5°), crisp sticker shadow. Round = pencil yellow with ink text; tag = red pen or grid green with white text. Max two per screen, never on body text, never for claims the client hasn't given.
