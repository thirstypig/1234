# Checked list

A list marked off with a red-pen check — the way a good teacher marks correct answers. Used for trust facts in the hero and in How it works. Check = `secondary` (red pen); text = `text-primary`.
