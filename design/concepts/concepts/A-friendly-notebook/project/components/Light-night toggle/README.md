# Light-night toggle

A pill with 🌙 / ☀️ that flips `data-theme` between `day` and `night` on `<html>`. Try it — this preview is live. Default follows the device (`prefers-color-scheme`); a manual choice is remembered per browser. Icon-only in the header, labelled in menus.
