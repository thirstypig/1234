# Booking call-to-action band

The bottom-of-page repeat of the hero's actions: a flat note card with an `h2`, one supporting line in `text-secondary`, and Book + Message. One per page, right before the footer.
