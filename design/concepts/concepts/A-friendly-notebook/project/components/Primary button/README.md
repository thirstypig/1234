# Primary button

The one tomato button — use it for booking and nothing else. Label is a verb phrase; `📅` leads. Height 52px (44px small). Fill `primary` → `primary-hover` → `primary-pressed`; label `on-primary`. At most one per screen region; on mobile it goes full width.
