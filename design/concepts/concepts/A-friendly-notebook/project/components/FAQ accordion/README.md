# FAQ accordion

Native `<details>` rows on `surface` with a ❓ marker, question in heading font, + / – on the right. Hover `secondary-hover`, pressed `secondary-pressed`. Answers use `body` and never exceed the 680px reading width. Several may be open at once.
