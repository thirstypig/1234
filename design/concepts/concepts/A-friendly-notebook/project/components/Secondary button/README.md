# Secondary button

Ink outline, transparent fill. Used for messaging, directions and every non-booking action. Hover and pressed add the `secondary-hover` / `secondary-pressed` wash. Disabled uses a dashed hairline and `text-secondary` — used for the Walnut clinic until it opens.
